"""Closed stage — interface stub. See :mod:`anchor.retarget` and pipeline/README.md."""

def retarget(*args, **kwargs):
    raise NotImplementedError(
        "anchor.retarget.feasibility is Anchor's closed retargeting method. "
        "Validation results are on the project page; the method is not public."
    )
